import Link from "next/link";import{and,eq,isNotNull,lte}from"drizzle-orm";import{getDb}from"../../../db";import{categories,posts}from"../../../db/schema";import{sanitizeArticleHtml}from"../../content-sanitizer";
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params,now=new Date().toISOString().slice(0,19).replace("T"," ");let post:null|{title:string;excerpt:string;content:string;categoryName:string|null;coverUrl:string;coverAlt:string;publishedAt:string|null}=null;try{[post]=await getDb().select({title:posts.title,excerpt:posts.excerpt,content:posts.content,categoryName:categories.name,coverUrl:posts.coverUrl,coverAlt:posts.coverAlt,publishedAt:posts.publishedAt}).from(posts).leftJoin(categories,eq(posts.categoryId,categories.id)).where(and(eq(posts.slug,slug),eq(posts.status,"published"),isNotNull(posts.publishedAt),lte(posts.publishedAt,now))).limit(1)}catch{}if(!post)return <main><article className="article shell"><Link className="back" href="/">← Ana sayfa</Link><h1>Yazı bulunamadı</h1></article></main>;const rich=/<[a-z][\s\S]*>/i.test(post.content),safe=rich?sanitizeArticleHtml(post.content):post.content.split(/\n{2,}/).map(value=>`<p>${value.trim().replace(/\n/g,"<br>")}</p>`).join("");return <main><header className="site-header shell"><Link className="brand" href="/"><span>OP8</span> OP8 Operating Partner Value Creation Framework</Link><nav><Link href="/#yazilar">Yazılar</Link><Link href="/#konular">Konular</Link></nav></header><article className="article shell"><Link className="back" href="/">← Tüm yazılar</Link><p className="kicker">{post.categoryName||"GENEL"}</p><h1>{post.title}</h1><p className="article-deck">{post.excerpt}</p>{post.coverUrl&&<img className="article-cover" src={post.coverUrl} alt={post.coverAlt}/>}<div className="article-rule"/><div className="article-body"><div className="article-rich-content" dangerouslySetInnerHTML={{__html:safe}}/></div></article></main>}

"use client";
import{useEffect,useState}from"react";import{defaultHomeConfig,HomeConfig,normalizeHomeConfig}from"../../homepage-config";import HeroCanvas from"./HeroCanvas";import HomeDesigner from"./HomeDesigner";
export default function HomeDesignerCanvas(){const[config,setConfig]=useState<HomeConfig>(defaultHomeConfig),[ready,setReady]=useState(false),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/admin/homepage").then(r=>r.json()).then(d=>{if(d.item)setConfig(normalizeHomeConfig(d.item));setReady(true)})},[]);async function save(){const r=await fetch("/api/admin/homepage",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(config)});setMessage(r.ok?"Hero yerleşimi kaydedildi.":"Kaydedilemedi.")}return <><div className="hero-layout-admin"><div className="canvas-admin-head"><div><p className="eyebrow">SERBEST YERLEŞİM</p><h1>Hero Yerleşim Tasarımı</h1><p>Metin ve buton alanlarını sürükleyin; sağ alt köşeden boyutlandırın.</p></div><button className="admin-primary top" onClick={save}>Yerleşimi kaydet</button></div>{message&&<p className="admin-message">{message}</p>}{ready&&<HeroCanvas config={config} onChange={layout=>setConfig({...config,hero:{...config.hero,layout}})}/>}</div><HomeDesigner/></>}

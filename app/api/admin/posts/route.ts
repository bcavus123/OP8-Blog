import{and,asc,desc,eq}from"drizzle-orm";import{getDb}from"../../../../db";import{categories,posts}from"../../../../db/schema";import{apiPermission,can}from"../../../admin-auth";
const slugify=(v:string)=>v.toLocaleLowerCase("tr-TR").normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/ı/g,"i").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,80);const values=(i:Record<string,unknown>)=>({title:String(i.title??"").trim(),slug:slugify(String(i.slug||i.title||"")),excerpt:String(i.excerpt??""),content:String(i.content??""),status:String(i.status??"draft"),categoryId:i.categoryId?Number(i.categoryId):null,coverUrl:String(i.coverUrl??""),coverAlt:String(i.coverAlt??""),seoTitle:String(i.seoTitle??""),seoDescription:String(i.seoDescription??""),publishedAt:i.publishedAt?String(i.publishedAt):null});
export async function GET(){const a=await apiPermission("posts.read");if(a.error)return a.error;const db=getDb(),own=a.actor.role==="author"?eq(posts.authorId,Number(a.actor.userId)):undefined;const list=await db.select({id:posts.id,title:posts.title,slug:posts.slug,excerpt:posts.excerpt,content:posts.content,status:posts.status,categoryId:posts.categoryId,authorId:posts.authorId,categoryName:categories.name,coverUrl:posts.coverUrl,coverAlt:posts.coverAlt,seoTitle:posts.seoTitle,seoDescription:posts.seoDescription,publishedAt:posts.publishedAt,updatedAt:posts.updatedAt}).from(posts).leftJoin(categories,eq(posts.categoryId,categories.id)).where(own).orderBy(desc(posts.updatedAt),desc(posts.id));return Response.json({posts:list,categories:await db.select().from(categories).orderBy(asc(categories.sortOrder)),capabilities:{publish:can(a.actor,"posts.publish"),delete:can(a.actor,"posts.delete"),ownOnly:a.actor.role==="author"}})}
export async function POST(r:Request){const a=await apiPermission("posts.write");if(a.error)return a.error;const v=values(await r.json());if(!v.title||!v.slug)return Response.json({error:"Başlık ve geçerli slug zorunludur."},{status:400});if(!can(a.actor,"posts.publish")){v.status="draft";v.publishedAt=null}try{const ids=await getDb().insert(posts).values({...v,authorId:a.actor.bootstrap?null:Number(a.actor.userId)}).$returningId();return Response.json({id:ids[0].id},{status:201})}catch{return Response.json({error:"Bu slug zaten kullanılıyor."},{status:409})}}
async function owned(id:number,actor:any){if(actor.role!=="author")return true;const[row]=await getDb().select({id:posts.id}).from(posts).where(and(eq(posts.id,id),eq(posts.authorId,Number(actor.userId))));return Boolean(row)}
export async function PUT(r:Request){const a=await apiPermission("posts.write");if(a.error)return a.error;const i=await r.json()as Record<string,unknown>,id=Number(i.id),v=values(i);if(!id||!v.title||!v.slug)return Response.json({error:"Eksik alan var."},{status:400});if(!await owned(id,a.actor))return Response.json({error:"Yalnızca kendi yazılarınızı düzenleyebilirsiniz."},{status:403});if(!can(a.actor,"posts.publish")){v.status="draft";v.publishedAt=null}try{await getDb().update(posts).set(v).where(eq(posts.id,id));return Response.json({ok:true})}catch{return Response.json({error:"Bu slug zaten kullanılıyor."},{status:409})}}
export async function PATCH(r:Request){const a=await apiPermission("posts.publish");if(a.error)return a.error;const{id,status}=await r.json()as{id?:number;status?:string};if(!id||!status)return Response.json({error:"Geçersiz işlem"},{status:400});await getDb().update(posts).set({status,publishedAt:status==="published"?new Date().toISOString().slice(0,19).replace("T"," "):null}).where(eq(posts.id,id));return Response.json({ok:true})}
export async function DELETE(r:Request){const a=await apiPermission("posts.delete");if(a.error)return a.error;const{id}=await r.json()as{id?:number};if(!id)return Response.json({error:"Yazı bulunamadı"},{status:400});await getDb().delete(posts).where(eq(posts.id,id));return Response.json({ok:true})}

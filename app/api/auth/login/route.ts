import{NextRequest,NextResponse}from"next/server";import{getPool}from"../../../../db";import{createUserSession,verifyPassword}from"../../../user-auth";
export async function POST(request:NextRequest){const form=await request.formData();const email=String(form.get("email")||"").trim().toLowerCase();const password=String(form.get("password")||"");const target=safe(String(form.get("return_to")||"/profil"));const[rows]=await getPool().execute<any[]>("SELECT id,password_hash FROM users WHERE email=? AND status='active' LIMIT 1",[email]);const user=rows[0];if(!user||!verifyPassword(password,user.password_hash))return NextResponse.redirect(new URL(`/giris?error=giris&return_to=${encodeURIComponent(target)}`,request.url),303);await createUserSession(user.id);return NextResponse.redirect(new URL(target,request.url),303)}function safe(v:string){return v.startsWith("/")&&!v.startsWith("//")?v:"/profil"}
